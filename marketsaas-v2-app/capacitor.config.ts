import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'marketsaas-v2-app',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
